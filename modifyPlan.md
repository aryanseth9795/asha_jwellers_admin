we have to add transaction  in rehan flow as we have in lenden data flow. user can add transaction and update transaction and delete transaction. same logic replicate from lenden to rehan section.

Also in customer table we have to add an optional field calld nickname and show it in customer list and customer detail screen. also give UI (optional field) to add nickname in existing customers or while adding new customer.